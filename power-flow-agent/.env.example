# Copy to .env and fill in. Do not commit .env / .env.local.
# Only needed for the LLM chat and the LLM-only baseline; the solver,
# visualization, N-1, and remedial features run without API keys.
OPENAI_API_KEY=your_openai_key_here
# GEMINI_API_KEY=your_gemini_key_here
