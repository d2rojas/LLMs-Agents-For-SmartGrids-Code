{
  "runner_name": "single_call:rag",
  "group": "Single-call tool use (paper Sec. 3, tools in one round)",
  "kind": "engine",
  "architecture": "single_call",
  "strategy": "rag",
  "uses_llm": true,
  "uses_tools": true,
  "gate": true,
  "final_gate": false,
  "preload_case": true,
  "description": "One function-calling round: the model must emit every tool call at once, no memory, no second turn. Structured prompt whose system-data block holds only the rows retrieved for the request (k=8).",
  "prompt_files": [
    "_shared/agent_system_prompt.txt",
    "single_call_structured/task_statement.txt",
    "single_call_structured/output_section.txt"
  ],
  "dynamic_parts": [
    "compact case rows and tool descriptions (llm/prompt_variants.py)"
  ],
  "folder": "single_call_rag"
}
