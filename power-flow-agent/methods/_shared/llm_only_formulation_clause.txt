
- Before the numbers, declare how you formulate the request in a `formulation` field of the same JSON object: the ordered list of solver operations a tool-using assistant would run to answer it, as {"tool": "<name>", "args": {...}} objects, using only the operation names and argument names listed under "## Formulation" in the user message. Declare it even when you set `converged` to false.