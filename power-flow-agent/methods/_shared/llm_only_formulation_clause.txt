
- The JSON object must start with a `formulation` field that declares how you formulate the request: the ordered list of solver operations a tool-using assistant would run to answer it, as {"tool": "<name>", "args": {...}} objects, using only the operation names and argument names listed under "## Formulation" in the user message. Declare it whether or not you can compute the numbers, including when you set `converged` to false.