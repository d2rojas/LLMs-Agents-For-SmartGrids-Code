## Formulation
State how you formulate the request as solver operations. The JSON object you return must START with a `formulation` field: an ordered list of {"tool": "<name>", "args": {...}} objects, one per operation the request needs, in execution order, using only the operations and argument names below (* = required). Copy identifiers and values from the request; do not add arguments the request does not give. This field declares your understanding of the request only. It does not change the rest of your answer: compute the numbers yourself if you can, or set `converged` to false if you cannot, exactly as the output requirements say.

Available operations:
