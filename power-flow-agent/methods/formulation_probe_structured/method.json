{
  "folder": "formulation_probe_structured",
  "runner_name": "formulation_probe:structured",
  "group": "Formulation probe (companion to the LLM-only prompting rows)",
  "kind": "llm_only",
  "strategy": "structured",
  "probe": true,
  "uses_llm": true,
  "uses_tools": false,
  "gate": false,
  "final_gate": false,
  "description": "Same case tables and request as llm_only:structured, but the only output is the declared formulation (the ordered solver operations the request needs). Fills the Formulation cell of the prompting rows without changing how those rows answer.",
  "prompt_files": [
    "_shared/formulation_probe_system_prompt.txt",
    "_shared/formulation_probe_section.txt",
    "_shared/formulation_probe_output_section.txt",
    "_shared/llm_only_user_template.txt"
  ],
  "dynamic_parts": [
    "system-data tables rendered from the perturbed case",
    "tool catalogue appended to '## Formulation' (llm/tools.py, --tool-variant)"
  ]
}
